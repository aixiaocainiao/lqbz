#include <stdio.h>
#include <ctype.h>
#include <stdlib.h>
#define COVERAGE 350
//#define SPACE ' '
//#define HUAN ''
int main()
{
	char ch;
	int ncount = 0;
	printf("please enter a list words.\n ");
	
	while (ch = getchar() != '\n')
	{
		if()



	}
	printf("一共有%d 个单词。"，ncount);

	char ch;
	while (ch = getchar() != '\n')
	{
		if (isalpha(ch))
			putchar(ch + 1);
		else
			putchar(ch);



	}
	putchar(ch);




	system("pause");
	return 0;
}





 max = (a > b) ? a : b;
 min = (a <b ) ? a : b;

 ? : 运算符;

#include <stdio.h>
#include <stdlib.h>
#define COVERAGE 350
int main()
{
	int suv;//一共粉刷的面积
	int cans = 0;//一共使用的罐数
	int stg = 0;//判断是否有余数
	printf("please enter the coverage of the useing.\n");

	scanf_s("%d ", &suv);
	cans = suv / COVERAGE;
	stg = suv % COVERAGE;

	if (stg == 0)

	    cans += stg;

	else
		cans ++；
	printf("the total cans is %d guan.\n",cans);









	system("pause");
	return 0;
}
#include <stdio.h0>
#include <stdlib.h>
int main()
{
	









	system("pause");

	return 0;
}